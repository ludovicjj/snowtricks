{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('css/form_trick.css') }}">
{% endblock %}

{% block body %}
    <div class="container mt-4">

        {# Message Flash #}
        <div class="row">
            <div class="col-12">
                {% for message in app.session.flashbag.get('success') %}
                    <div class="col-12">
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                    </div>
                {% endfor %}

                {% for message in app.session.flashbag.get('reset-success') %}
                    <div class="col-12">
                        <div class="alert alert-success">
                            {{ message }}
                        </div>
                    </div>
                {% endfor %}

                {% if error %}
                    <div class="col-12">
                        <div class="alert alert-danger">
                            {{ error.messageKey|trans(error.messageData, 'security') }}
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>

        <form method="post">

            <div class="form-row">
                <div class="col-12">
                    <div class="col-md-12 page-header">
                        <h4>Connexion</h4>
                    </div>
                </div>
            </div>

            <div class="form-row">
                <div class="col-12">
                    <div class="col-md-12 form-group">
                        {# Username #}
                        <label for="inputUsername">Pseudo</label>
                        <input type="text"
                               name="username"
                               id="inputUsername"
                               class="form-control"
                               value="{{ last_username }}"
                               required autofocus
                        >
                    </div>
                    <div class="col-md-12 form-group">
                        {# password #}
                        <label for="inputPassword">Mot de passe</label>
                        <input type="password"
                               name="password"
                               id="inputPassword"
                               class="form-control"
                               required
                        >
                    </div>
                    {# csrf_token #}
                    <input type="hidden"
                           name="_csrf_token"
                           value="{{ csrf_token('snowtricksauthentification') }}"
                    >
                </div>
            </div>

            <div class="form-row">
                <div class="col-12">
                    <div class="col-12">
                        <a href="{{ path('security_forgotten') }}">Mot de passe oublié</a>
                    </div>
                </div>
            </div>

            <div class="form-row">
                <div class="col-12" style="margin-left:15px;">
                    <button type="submit" class="btn-secondary btn mt-2">
                        Connexion
                    </button>
                </div>
            </div>

        </form>
    </div>
{% endblock %}